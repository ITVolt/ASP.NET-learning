INSERT INTO dbo.users
(
    alias,
    first_name,
    last_name,
    email,
    registration_date,
    last_active
	)
VALUES
(   'Haggis',        -- alias - varchar(255)
    'Scott',        -- first_name - varchar(255)
    'Sheeply',        -- last_name - varchar(255)
    'drop@tables',        -- email - varchar(255)
    GETDATE(), -- registration_date - datetime
    GETDATE()  -- last_active - datetime
    )

	INSERT INTO dbo.users
(
    alias,
    first_name,
    last_name,
    email,
    registration_date,
    last_active
)
VALUES
(   'Killer Joe',        -- alias - varchar(255)
    'Joanne',        -- first_name - varchar(255)
    'D`Arc',        -- last_name - varchar(255)
    'drop@tables',        -- email - varchar(255)
    GETDATE(), -- registration_date - datetime
    GETDATE()  -- last_active - datetime
    )
	INSERT INTO dbo.users
(
    alias,
    first_name,
    last_name,
    email,
    registration_date,
    last_active
)
VALUES
(   'Lord of the cals',        -- alias - varchar(255)
    'Jebus',        -- first_name - varchar(255)
    'Ishkariot',        -- last_name - varchar(255)
    'drop@tables',        -- email - varchar(255)
    GETDATE(), -- registration_date - datetime
    GETDATE()  -- last_active - datetime
    )

	INSERT INTO dbo.users
(
    alias,
    first_name,
    last_name,
    email,
    registration_date,
    last_active
)
VALUES
(   '<a>Hello</a>',        -- alias - varchar(255)
    'First name',        -- first_name - varchar(255)
    'Some name',        -- last_name - varchar(255)
    'drop@tables',        -- email - varchar(255)
    GETDATE(), -- registration_date - datetime
    GETDATE()  -- last_active - datetime
    )

	INSERT INTO dbo.users
(
    alias,
    first_name,
    last_name,
    email,
    registration_date,
    last_active
)
VALUES
(   'Hot Dawg',        -- alias - varchar(255)
    'Snoop',        -- first_name - varchar(255)
    'DeLoop',        -- last_name - varchar(255)
    'weed@err.day',        -- email - varchar(255)
    GETDATE(), -- registration_date - datetime
    GETDATE()  -- last_active - datetime
    )